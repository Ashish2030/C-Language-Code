writes codes for function sub&add which will be used to subtract and add element of 2 arrays resp.the resultant
arrays that is arrays recieved after subtraction and additionare global and named as s[]&p[] resp. we are
assuming that resultant array cannot have more than 100 elements and 2 arrays which will be subt. or added are 
of equal size,the function sub and add will recieve the 2 arrays and size of arrays (both arrays are of same size).
